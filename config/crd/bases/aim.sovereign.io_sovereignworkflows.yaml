---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: sovereignworkflows.aim.sovereign.io
spec:
  group: aim.sovereign.io
  names:
    kind: SovereignWorkflow
    listKind: SovereignWorkflowList
    plural: sovereignworkflows
    singular: sovereignworkflow
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SovereignWorkflow is the Schema for the Sovereign workflows API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SovereignWorkflowSpec defines the desired state (The user's
              intent or ArgoCD's deployment)
            properties:
              classification:
                type: string
              definitionRevision:
                type: string
              projectRef:
                type: string
              requestedVolumeSize:
                type: string
              steps:
                items:
                  description: StepConfig defines what a specific agent needs
                  properties:
                    capabilities:
                      items:
                        type: string
                      type: array
                    deterministic:
                      type: boolean
                    evictable:
                      type: boolean
                    executable:
                      items:
                        type: string
                      type: array
                    responsibility:
                      type: string
                    image:
                      type: string
                    inputs:
                      items:
                        properties:
                          digest:
                            type: string
                          name:
                            type: string
                        required:
                        - name
                        type: object
                      type: array
                    kind:
                      type: string
                    maxAttempts:
                      format: int32
                      type: integer
                    modelName:
                      type: string
                    modelRevision:
                      type: string
                    name:
                      type: string
                    order:
                      type: integer
                    outputs:
                      items:
                        properties:
                          name:
                            type: string
                          version:
                            type: string
                        required:
                        - name
                        - version
                        type: object
                      type: array
                    priority:
                      format: int32
                      type: integer
                    requestedVRAMAllocation:
                      format: int64
                      type: integer
                    sharingScope:
                      type: string
                    timeout:
                      type: string
                  required:
                  - responsibility
                  - kind
                  - name
                  - order
                  type: object
                type: array
              workflowId:
                type: string
            required:
            - projectRef
            - requestedVolumeSize
            - steps
            - workflowId
            type: object
          status:
            description: SovereignWorkflowStatus defines the observed state (Written
              ONLY by your Orchestrator)
            properties:
              activeAttemptRef:
                type: string
              activeStep:
                type: string
              allocatedNode:
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
              phase:
                type: string
              pvcName:
                type: string
              requestedVRAMAllocation:
                format: int64
                type: integer
              stepStatuses:
                items:
                  properties:
                    assignedNode:
                      type: string
                    gpuIndex:
                      type: string
                    inferenceEndpointUrl:
                      type: string
                    inferencePodName:
                      type: string
                    phase:
                      description: |-
                        SovereignWorkflow (The CRD Object)

                        ├── Spec (The Intent)
                        │    └── Steps []StepConfig (Array of nested structs)
                        └── Status (The Reality)
                        └── StepRuntime []StepRuntimeStatus (Where you track active execution data)
                      type: string
                    podName:
                      type: string
                    stepName:
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
