package tests

import (
	"testing"
	//"github.com/SovereignAI/internal/kube"
	//"k8s.io/client-go/kubernetes/fake"
)

func TestReconcile(t *testing.T) {
	// Fake empty clientset
	//fakeKube := fake.NewSimpleClientset()

	//manager := kube.NewKubeManager(fakeKube)

	//orc := NewOrchestrator(manager, store)
}
