apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Add another child only after that directory contains a renderable
  # kustomization and its referenced manifests.
  - postgresql
