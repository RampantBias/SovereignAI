apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../config/crd/bases
  - ../base
  - postgresql
  - registry
  - argocd
  - nvidia-device-plugin
  - model-cache

# Prometheus is intentionally a standalone optional package:
#   kubectl apply -k deploy/demo/prometheus
