# Model cache

This package creates only the retained `sovereign-model-cache` PVC. Model
selection, immutable revision checksums, preload, and offline verification are
completed with the inference work in P4.2/P4.8. No model token or model bytes
belong in this repository.
