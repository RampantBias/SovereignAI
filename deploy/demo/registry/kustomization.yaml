apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sovereign-registry

resources:
  - namespace.yaml
  - service.yaml
  - statefulset.yaml
  - configuration.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: sovereign-ai-demo
      app.kubernetes.io/component: registry