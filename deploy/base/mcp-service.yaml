apiVersion: v1
kind: Service
metadata:
  name: sovereign-mcp
  namespace: sovereign-orchestrator-system
  labels:
    app.kubernetes.io/name: sovereign-mcp
spec:
  selector:
    app.kubernetes.io/name: sovereign-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
