apiVersion: v1
kind: Service
metadata:
  name: sovereign-controller-metrics
  namespace: sovereign-orchestrator-system
  labels:
    app.kubernetes.io/name: sovereign-controller
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sovereign-controller
  ports:
    - name: metrics
      port: 8081
      targetPort: metrics
